<x-admin.layout heading="Data Jadwal"><div class="flex justify-end mb-5"><a class="bg-teal-700 text-white px-4 py-2 rounded-xl" href="{{ route('admin.schedules.create') }}">+ Tambah Jadwal</a></div><div class="bg-white border rounded-2xl overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-50 text-left"><tr><th class="p-4">Bus</th><th>Hari</th><th>Jam</th><th></th></tr></thead><tbody>@foreach($schedules as $schedule)<tr class="border-t"><td class="p-4 font-bold">{{ $schedule->bus->name }}</td><td>{{ $schedule->day_type }}</td><td>{{ substr($schedule->start_time,0,5) }}–{{ substr($schedule->end_time,0,5) }}</td><td class="p-4 text-right"><a class="text-teal-700" href="{{ route('admin.schedules.edit',$schedule) }}">Ubah</a><form class="inline" method="post" action="{{ route('admin.schedules.destroy',$schedule) }}">@csrf @method('DELETE')<button class="ml-3 text-red-600">Hapus</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $schedules->links() }}</div></x-admin.layout>
