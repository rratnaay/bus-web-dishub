<x-admin.layout heading="Data Rute"><div class="flex justify-end mb-5"><a class="bg-teal-700 text-white px-4 py-2 rounded-xl" href="{{ route('admin.routes.create') }}">+ Tambah Rute</a></div><div class="bg-white border rounded-2xl overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-50 text-left"><tr><th class="p-4">Rute</th><th>Bus</th><th>Arah</th><th></th></tr></thead><tbody>@foreach($routes as $route)<tr class="border-t"><td class="p-4 font-bold">{{ $route->name }}</td><td>{{ $route->bus->name }}</td><td>{{ $route->direction }}</td><td class="p-4 text-right"><a class="text-teal-700" href="{{ route('admin.routes.edit',$route) }}">Ubah</a><form class="inline" method="post" action="{{ route('admin.routes.destroy',$route) }}">@csrf @method('DELETE')<button class="ml-3 text-red-600">Hapus</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $routes->links() }}</div></x-admin.layout>
