<x-admin.layout heading="Data Halte"><div class="flex justify-end mb-5"><a class="bg-teal-700 text-white px-4 py-2 rounded-xl" href="{{ route('admin.stops.create') }}">+ Tambah Halte</a></div><div class="bg-white border rounded-2xl overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-50 text-left"><tr><th class="p-4">Halte</th><th>Lokasi</th><th></th></tr></thead><tbody>@foreach($stops as $stop)<tr class="border-t"><td class="p-4 font-bold">{{ $stop->name }}<span class="block font-normal text-slate-500">{{ $stop->code }}</span></td><td>{{ $stop->latitude }}, {{ $stop->longitude }}</td><td class="p-4 text-right"><a class="text-teal-700" href="{{ route('admin.stops.edit',$stop) }}">Ubah</a><form class="inline" method="post" action="{{ route('admin.stops.destroy',$stop) }}">@csrf @method('DELETE')<button class="ml-3 text-red-600">Hapus</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $stops->links() }}</div></x-admin.layout>
