<x-admin.layout heading="Dashboard"><div class="grid gap-5 sm:grid-cols-2 xl:grid-cols-4">@foreach($statistics as $stat)<article class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><div class="flex items-start justify-between"><div><p class="text-sm font-medium text-slate-500">{{ $stat['label'] }}</p><p class="mt-2 text-3xl font-extrabold text-slate-900">{{ $stat['value'] }}</p></div><span class="grid h-12 w-12 place-items-center rounded-2xl bg-{{ $stat['color'] }}-50 text-xl">{{ $stat['icon'] }}</span></div><p class="mt-4 text-xs font-semibold text-emerald-600">Data tercatat saat ini</p></article>@endforeach</div><section class="mt-8 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="flex items-center justify-between border-b border-slate-100 px-6 py-5"><div><h2 class="font-bold text-slate-900">Aktivitas terbaru</h2><p class="mt-1 text-sm text-slate-500">Bus yang terakhir ditambahkan atau diperbarui.</p></div><a href="{{ route('admin.buses.index') }}" class="text-sm font-bold text-blue-600">Kelola Bus →</a></div><div class="overflow-x-auto"><table class="w-full min-w-[540px] text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th class="px-6 py-4">Bus</th><th class="px-6 py-4">Nomor Jalur</th><th class="px-6 py-4">Status</th><th class="px-6 py-4">Diperbarui</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($recentBuses as $bus)<tr><td class="px-6 py-4 font-semibold">{{ $bus->name }}</td><td class="px-6 py-4"><span class="rounded-lg px-2.5 py-1 text-xs font-bold text-white" style="background:{{ $bus->color }}">{{ $bus->corridor_number }}</span></td><td class="px-6 py-4"><span class="rounded-full {{ $bus->is_active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-600' }} px-2.5 py-1 text-xs font-semibold">{{ $bus->is_active ? 'Aktif' : 'Tidak Aktif' }}</span></td><td class="px-6 py-4 text-slate-500">{{ $bus->updated_at->diffForHumans() }}</td></tr>@empty<tr><td colspan="4" class="px-6 py-10 text-center text-slate-500">Belum ada aktivitas data bus.</td></tr>@endforelse</tbody></table></div></section></x-admin.layout>
