<x-admin.layout heading="Data Bus"><div class="flex justify-between mb-5"><form><input name="q" value="{{ request('q') }}" class="rounded-xl border-slate-200" placeholder="Cari bus..."></form><a class="bg-teal-700 text-white px-4 py-2 rounded-xl" href="{{ route('admin.buses.create') }}">+ Tambah Bus</a></div><div class="bg-white border rounded-2xl overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-50 text-left"><tr><th class="p-4">Bus</th><th>Koridor</th><th>Status</th><th></th></tr></thead><tbody>@foreach($buses as $bus)<tr class="border-t"><td class="p-4 font-bold">{{ $bus->name }}<span class="block font-normal text-slate-500">{{ $bus->code }}</span></td><td>{{ $bus->corridor_number }}</td><td>{{ $bus->is_active?'Aktif':'Nonaktif' }}</td><td class="p-4 text-right"><a class="text-teal-700" href="{{ route('admin.buses.edit',$bus) }}">Ubah</a><form class="inline" method="post" action="{{ route('admin.buses.destroy',$bus) }}">@csrf @method('DELETE')<button class="ml-3 text-red-600" onclick="return confirm('Hapus bus?')">Hapus</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $buses->links() }}</div></x-admin.layout>
