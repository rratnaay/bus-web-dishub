<x-layouts.app title="{{ $bus->name }} | BIS Hub"><section class="max-w-6xl mx-auto px-5 py-12"><a class="text-teal-700" href="{{ route('search') }}">← Kembali ke pencarian</a><div class="mt-5 grid lg:grid-cols-3 gap-8"><div class="lg:col-span-2"><div class="bg-white rounded-2xl p-7 border"><span class="font-bold text-sm px-3 py-1 rounded-full text-white" style="background:{{ $bus->color }}">KORIDOR {{ $bus->corridor_number }}</span><h1 class="text-3xl font-black mt-4">{{ $bus->name }}</h1><p class="text-slate-500 mt-3">{{ $bus->description }}</p><div id="route-map" class="mt-6 rounded-xl"></div></div></div><aside class="space-y-5"><div class="bg-white rounded-2xl p-6 border"><h2 class="font-bold text-lg">Halte yang dilalui</h2><ol class="mt-4 border-l-2 border-teal-200 ml-2 space-y-4">@foreach($bus->routes->flatMap->stops->unique('id') as $stop)<li class="pl-5 relative"><i class="absolute -left-2 top-1 w-3 h-3 rounded-full bg-teal-600"></i><b>{{ $stop->pivot->sequence }}. {{ $stop->name }}</b><p class="text-xs text-slate-500">{{ $stop->address }}</p></li>@endforeach</ol></div><div class="bg-white rounded-2xl p-6 border"><h2 class="font-bold">Jam operasional</h2>@forelse($bus->schedules as $s)<p class="mt-3 text-sm">{{ $s->day_type }}<br><b>{{ substr($s->start_time,0,5) }}–{{ substr($s->end_time,0,5) }}</b> @if($s->headway_minutes) · tiap {{ $s->headway_minutes }} menit @endif</p>@empty<p class="mt-2 text-sm text-slate-500">Belum tersedia.</p>@endforelse</div></aside></div></section><x-slot:scripts><script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script><script>const stops=@json($bus->routes->flatMap->stops->unique('id')->values());const map=L.map('route-map');const points=stops.map(s=>[Number(s.latitude),Number(s.longitude)]);if(points.length){L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap'}).addTo(map);L.polyline(points,{color:'{{ $bus->color }}',weight:5}).addTo(map);points.forEach((p,i)=>L.marker(p).addTo(map).bindPopup((i+1)+'. '+stops[i].name));map.fitBounds(points,{padding:[30,30]});}else map.setView([-6.2,106.816],12);</script></x-slot:scripts></x-layouts.app>
