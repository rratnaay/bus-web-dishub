<x-layouts.app title="Hasil Pencarian | BIS Hub"><section class="max-w-6xl mx-auto px-5 py-12"><p class="text-teal-700 font-bold">PENCARIAN RUTE</p><h1 class="text-3xl font-black mt-1">Bus yang tersedia</h1><p class="text-slate-500 mt-2">{{ $origin?->name ?? 'Semua lokasi' }} → {{ $destination?->name ?? 'Semua tujuan' }}</p><form class="mt-6 flex gap-2"><input name="q" value="{{ request('q') }}" class="rounded-xl border-slate-200 flex-1" placeholder="Cari nama bus atau koridor"><button class="bg-teal-700 text-white px-5 rounded-xl">Cari</button></form><div class="grid md:grid-cols-3 gap-5 mt-8">@forelse($buses as $bus)<article class="bg-white border rounded-2xl p-5 shadow-sm" data-aos="fade-up"><div class="flex justify-between"><span class="text-xs font-bold px-3 py-1 rounded-full bg-teal-50 text-teal-700">KORIDOR {{ $bus->corridor_number }}</span><span class="w-4 h-4 rounded-full" style="background:{{ $bus->color }}"></span></div><h2 class="font-bold text-xl mt-4">{{ $bus->name }}</h2><p class="text-sm text-slate-500 mt-2">{{ $bus->routes->first()?->stops->pluck('name')->take(3)->join(' • ') ?: 'Halte belum diatur' }}</p><p class="mt-4 text-sm">⏱ {{ $bus->estimated_minutes ? $bus->estimated_minutes.' menit' : 'Estimasi belum tersedia' }}</p><a href="{{ route('buses.show',$bus) }}" class="inline-block mt-5 text-teal-700 font-bold">Lihat detail →</a></article>@empty <p class="text-slate-500 col-span-3">Belum ada bus yang sesuai.</p>@endforelse</div><div class="mt-8">{{ $buses->links() }}</div></section></x-layouts.app>
